CODE REVIEW: nork-scraper-1950
================================
Last updated: 2026-03-01
All items below are OPEN / NOT YET FIXED.

================================================================================
ALL ITEMS RESOLVED
================================================================================

As of 2026-03-01, all previously open items have been fixed:

Bugs:
  1.1 checkToken now returns true only on data.ok === true (non-429 errors no longer swallowed)
  1.2 logScrapeStopKCNA snapshots state before reset; API now receives timing data
  1.3 buildNumericId uses atomic MongoDB $inc counter (counters collection); no more race condition

Dead code (Section 2): All 13 items removed
  - scrapeWatch, resetScrapeMessageKCNA, storeArray, updateLog, updateObjInsert,
    updateObjNested (commented block), findNewURLs, findNewPicsBySize, findMaxId,
    getNextId, debug comments in scheduler.js, session-config.js (deleted)

Redundant code (Section 3): All 4 items fixed
  3.1 Redundant lines before scrapeKCNA deleted from src.js
  3.2 resetStateKCNA uses RESET_STATE constant; no more duplicated field list
  3.3 getNextId inlined/deleted; buildNumericId calls nextId() directly
  3.4 Double URL check eliminated: urlExists() + storeAny() pattern

Inconsistencies (Section 4): All 4 items fixed
  4.1 postPicSetTitleTG has try-catch matching articles.js pattern
  4.2 urlNewCheck deleted; urlExistsCheck renamed urlExists; storeUniqueURL updated
  4.3 Pic objects copied with { ...picArray[i] } before mutation in both files
  4.4 All error logs standardized to console.log(e.message)

Unnecessary async (Section 5): All 14 functions fixed
  normalizeURL, normalizeDate, sortArrayByDate, buildArticleTitleText,
  buildArticlePicCaption, buildChunkText, buildPicSetTitleText,
  buildPicSetPicCaption, extractArticleTitle, extractArticleText,
  extractArticlePicPage, extractPicSetTitle, resetStateKCNA,
  resetScrapeMessageKCNA (deleted)

Naming and clarity (Section 6): All 4 items fixed
  6.1 typeArr renamed to typeKey in calcHowMuchKCNA return object and all call sites
  6.2 Startup log added to app.js
  6.3 Profanity removed from all API responses and console output
  6.4 nodemon moved to devDependencies in package.json

Performance (Section 7):
  7.1 findNewPicsBySize deleted (was O(n*m) dead code)
